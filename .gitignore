# OS / editor
.DS_Store
Thumbs.db
desktop.ini
*.swp
.vscode/
.idea/

# temp / build
node_modules/
dist/
*.log

# wrangler local state — caches the Cloudflare account id, keep it out of git
.wrangler/

# reference frames pulled from screen recordings (not part of the site)
_refframes/
_ref2/

# local safety backups
*.bak

# v2 rebuild — local only, not deployed
hephoratech-v2/

# pre-optimisation image originals (local only)
*.orig
